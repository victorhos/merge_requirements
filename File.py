#!/usr/bin/env python
# encoding: utf-8

import sys

class ManageFile(object):

    def __init__(self, fm, ff, sf):

        self.file_merged = fm
        self.first_file = ff
        self.second_file = sf

    def main():

        print(self.file_merged)
